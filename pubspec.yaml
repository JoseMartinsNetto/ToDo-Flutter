name: todo
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter_modular: ^6.0.4
  uno: ^1.1.9
  
  flutter:
    sdk: flutter

dev_dependencies:
  mocktail: ^1.0.0
  flutterando_analysis: ^0.0.2

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
